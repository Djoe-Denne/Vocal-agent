[env]
# whisper-rs-sys/cmake on Windows may autodetect an invalid future VS generator.
# Pin to the installed generator for stable local builds.
CMAKE_GENERATOR = "Visual Studio 17 2022"
# CUDA 13.x + CCCL expects the standard-conforming MSVC preprocessor.
CL = "/Zc:preprocessor"
