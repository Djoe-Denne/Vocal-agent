# Push-to-Talk Speech-to-Text (Windows) Dependencies

# =============================================================================
# Core Dependencies
# =============================================================================

# Audio recording
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.1

# Speech-to-text transcription (Whisper backend)
openai-whisper>=20231117

# Speech-to-text transcription (HuggingFace backend - Qwen3-ASR, etc.)
# These are also used by the LLM reconciler
# Note: Qwen3-ASR uses the dedicated qwen-asr package
qwen-asr>=0.0.6
transformers==4.57.6
accelerate>=0.24.0

# Audio file loading (optional, improves HuggingFace transcriber audio handling)
librosa>=0.10.0

# Clipboard image capture
Pillow>=10.0.0

# HTTP client (daemon API)
requests>=2.32.0
# API server
fastapi>=0.110.0
uvicorn>=0.24.0

# Hotkey detection
pynput>=1.7.6

# TOML parsing (for Python < 3.11, which lacks tomllib)
tomli>=2.0.0;python_version<"3.11"

# =============================================================================
# Reconciler Dependencies (Optional - install based on chosen algorithm)
# =============================================================================

# For fuzzy reconciler (algorithm = "fuzzy"):
rapidfuzz>=3.0.0

# Note: LLM reconciler uses transformers/accelerate listed above

# =============================================================================
# PyTorch with CUDA support (recommended for GPU inference)
# =============================================================================
# Install PyTorch for cu128:
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Note: Your NVIDIA driver doesn't need to change - drivers are backward compatible.
#
# Verify GPU is detected:
#   python -c "import torch; print('CUDA:', torch.cuda.is_available())"
# =============================================================================
