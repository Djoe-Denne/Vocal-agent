//! Web API adapter for TTS (stub).
//!
//! Axum-based REST endpoints. This is a placeholder — full implementation
//! will be wired in a follow-up.
//!
//! Planned endpoints:
//! - `POST /synthesize` — JSON request body, returns WAV audio

// Stub — no implementation yet. The module exists so `lib.rs` can
// conditionally export it under the `web` feature gate.
